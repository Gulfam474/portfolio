# Use relative /api/v1 in Vite (proxied to backend). Or absolute e.g. http://localhost:8000/api/v1
VITE_API_BASE_URL=/api/v1
# Absolute API host for Google OAuth (must match GOOGLE_REDIRECT_URI host)
VITE_API_ORIGIN=http://localhost:8001
VITE_GOOGLE_CLIENT_ID=
